# ========================================
# INFRASTRUCTURE CONFIGURATION
# Copy this file to .env and update with your actual values
# ========================================

# ========================================
# DOMAIN CONFIGURATION
# ========================================

# Infrastructure Domain (where infrastructure services run)
# This domain hosts: traefik, webhook, status (all VPN-restricted)
INFRASTRUCTURE_DOMAIN=your-infrastructure-domain.com

# Project Domain (where your apps run)  
# Your apps will be available at: app.your-infrastructure-domain.com
DOMAIN=your-infrastructure-domain.com

# ========================================
# DATABASE CONFIGURATION
# ========================================

# MariaDB Root Password (automatically generated during install)
MYSQL_ROOT_PASSWORD=your_secure_root_password_here_min_16_chars

# ========================================
# MONITORING CONFIGURATION  
# ========================================

# Grafana Admin Credentials (automatically generated during install)
GRAFANA_USER=admin
GRAFANA_PASSWORD=your_secure_grafana_password_here_min_16_chars

# ========================================
# SSL CONFIGURATION
# ========================================

# Let's Encrypt email for certificate notifications
ACME_EMAIL=admin@your-infrastructure-domain.com

# ========================================
# LOGGING CONFIGURATION
# ========================================

# Traefik log level (ERROR, WARN, INFO, DEBUG)
TRAEFIK_LOG_LEVEL=INFO

# ========================================
# SECURITY CONFIGURATION
# ========================================

# Traefik Admin Authentication (automatically generated during install)
# Format: username:hashed_password (use htpasswd to generate)
TRAEFIK_AUTH=admin:$$2y$$10$$your_secure_bcrypt_hash_here

# ========================================
# SECURITY NOTES
# ========================================
# 
# 🔒 All infrastructure endpoints (traefik, status, prometheus) are VPN-restricted
# 🔒 Database users are automatically created per project/environment  
# 🔒 Passwords are automatically generated during installation (25 chars, base64)
# 🔒 No default database or user - only root user created
# 🔒 Regularly rotate passwords and update this file
# 
# Manual password generation (if needed):
# openssl rand -base64 32 | tr -d "=+/" | cut -c1-25
#
# Infrastructure endpoints (VPN only):
# - https://traefik.${INFRASTRUCTURE_DOMAIN} (Traefik dashboard)
# - https://status.${INFRASTRUCTURE_DOMAIN} (Grafana status dashboard)  
# - https://prometheus.${INFRASTRUCTURE_DOMAIN} (Prometheus metrics)
# 
# Public endpoints:
# - https://webhook.${INFRASTRUCTURE_DOMAIN} (Webhook receiver)
# - https://yourapp.${DOMAIN} (Your applications)
# ========================================